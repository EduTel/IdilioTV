import Fontisto from '@react-native-vector-icons/fontisto';
import { SafeAreaView } from 'react-native-safe-area-context';

const ErrorApi = () => {
  return (
    <SafeAreaView>
      <Fontisto name="eraser" color="black" size={24} />
    </SafeAreaView>
  );
};

export default ErrorApi;
